# Frontend Scaffold

This is a minimal Next.js app router scaffold for the HanilMatch project.

## Pages

- `/` landing page
- `/signup` signup form wired to the backend signup endpoint
- `/login` login form wired to the backend auth endpoint

## Next steps

1. Move from basic cookie session to refresh-token or rotating session strategy.
2. Add profile setup flow after signup.
3. Add protected dashboard and match list.
4. Add chat UI and report flow.
